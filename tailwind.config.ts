// Tailwind CSS v4 uses CSS-based configuration via @source and @theme directives
// See app/globals.css for configuration
// This file is kept for compatibility but configuration is done in CSS
import type { Config } from "tailwindcss";

const config: Config = {};
export default config;

